# Core application
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
pydantic-settings>=2.2.0
httpx>=0.27.0
SQLAlchemy>=2.0.29
jsonschema>=4.21.1
python-dotenv>=1.0.1
python-multipart>=0.0.9

# Testing
pytest>=8.1.1
pytest-asyncio>=0.23.6
pytest-cov>=5.0.0

# ------------------------------------------------------------------
# Optional integrations (Pluggable Architecture).
# None of these are required to run AVaaS - every one of them is imported
# lazily, inside a try/except, and the corresponding feature silently falls
# back to a built-in equivalent if the package or its backing service isn't
# available. Uncomment / install what you actually intend to use.
# ------------------------------------------------------------------

# Commercial LLM fallback (Gemini) - only needed if LLM_PROVIDER=gemini.
# AVaaS talks to Gemini over plain REST (httpx, already above), so no SDK
# is required even for the Gemini path.

# DeepEval - only needed if USE_DEEPEVAL=true.
# deepeval>=1.0.0

# Langfuse - only needed if LANGFUSE_PUBLIC_KEY/LANGFUSE_SECRET_KEY are set.
# langfuse>=2.0.0

# OpenTelemetry OTLP exporter - only needed if OTEL_EXPORTER_OTLP_ENDPOINT is set.
# opentelemetry-sdk>=1.24.0
# opentelemetry-exporter-otlp>=1.24.0

# LangSmith - only needed if LANGSMITH_API_KEY is set.
# langsmith>=0.1.0
