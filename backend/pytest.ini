[pytest]
# Anchor rootdir to backend/ so `app` is importable, regardless of whether
# pytest is invoked from here or via `make test` from the repo root.
testpaths = tests
pythonpath = .
asyncio_mode = auto
