{
  "_comment": "Example Target.config for a live REST/FastAPI agent. Supply via the dashboard's Advanced Config field or the API.",
  "path": "/chat",
  "method": "POST",
  "prompt_field": "message",
  "session_field": "session_id",
  "response_path": "response",
  "headers": {"X-Env": "staging"},
  "timeout": 60,
  "max_retries": 2,
  "backoff_seconds": 0.5,
  "rate_limit_per_sec": 0
}
